{
  "name": "web-crawler",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "install": "PUPPETEER_CACHE_DIR=./node_modules/.puppeteer-cache npm i --omit=dev"
  },
  "dependencies": {
    "puppeteer": "^24.43.1"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
